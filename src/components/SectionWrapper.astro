---
export interface Props {
  id: string;
  className?: string;
}

const { id, className = '' } = Astro.props;
---

<section
  id={id}
  class={`portfolio-section bg-bg ${className}`}
>
  <div class="max-w-5xl mx-auto px-6 py-24">
    <slot />
  </div>
</section>
